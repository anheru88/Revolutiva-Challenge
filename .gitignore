# Archivos de entorno generados por ./run.sh (solo se versiona .env.example)
/.env
/.env.tmp
/backend/.env

# Red de seguridad: la raíz no es un proyecto PHP (el backend vive en backend/)
/vendor/
/composer.json
/composer.lock

.DS_Store
